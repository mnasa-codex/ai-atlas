import { $ } from "../lib/dom.js";\nexport async function initCatalog() { const target = $("[data-catalog]"); if (!target) return; const response = await fetch("/data/index.json"); const items = await response.json(); target.innerHTML = items.map((item) => '<article class="catalog-card"><h2>' + item.name.ar + '</h2><p>' + item.tagline.ar + '</p><a href="/tools/' + item.slug + '.html">عرض التفاصيل</a></article>').join(""); }\n