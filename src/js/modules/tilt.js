import { $$, reduced } from '../lib/dom.js';
export function initTilt(){if(reduced()||!matchMedia('(hover:hover)').matches)return{destroy(){}};const nodes=new Set([...$$('.tilt3d'),...$$('.spot')]);const onMove=e=>{const el=e.currentTarget,r=el.getBoundingClientRect(),x=e.clientX-r.left,y=e.clientY-r.top;if(el.classList.contains('tilt3d')){const max=parseFloat(getComputedStyle(el).getPropertyValue('--tilt-max'))||6;el.style.setProperty('--ry',`${((x/r.width)*2-1)*max}deg`);el.style.setProperty('--rx',`${(1-(y/r.height)*2)*max}deg`)}el.style.setProperty('--mx',`${x}px`);el.style.setProperty('--my',`${y}px`)};const enter=e=>e.currentTarget.classList.contains('tilt3d')&&e.currentTarget.classList.add('is-live');const leave=e=>{const el=e.currentTarget;el.classList.remove('is-live');el.style.setProperty('--rx','0deg');el.style.setProperty('--ry','0deg');el.style.removeProperty('--mx');el.style.removeProperty('--my')};nodes.forEach(el=>{el.addEventListener('pointermove',onMove);el.addEventListener('pointerenter',enter);el.addEventListener('pointerleave',leave)});return{destroy(){nodes.forEach(el=>{el.removeEventListener('pointermove',onMove);el.removeEventListener('pointerenter',enter);el.removeEventListener('pointerleave',leave)})}}}