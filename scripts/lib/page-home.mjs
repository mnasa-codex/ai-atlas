import { esc, escAttr, L, bidi, urlFor, money } from './util.mjs';
import { icon } from './icons.mjs';
import { renderLayout } from './layout.mjs';
function renderLedgerRow(ctx,lang,tool){const{t,defaultLang,languages}=ctx;const locale=languages.find(l=>l.code===lang)?.locale||'ar-SA';const href=urlFor(lang,defaultLang,`tools/${tool.slug}.html`);const plans=[...(tool.pricing?.plans||[])].sort((a,b)=>(a.order??0)-(b.order??0));const logo=tool.media?.logo?.url;const priceOf=p=>p.is_free?{text:t(lang,'pricing.free'),contact:false}:p.contact_for_price===true||p.price?.amount==null?{text:t(lang,'pricing.contactBadge'),contact:true}:{text:money(p.price.amount,p.price.currency,locale),contact:false};const tiers=plans.map(p=>{const x=priceOf(p),h=p.what_you_get?.headline?L(p.what_you_get.headline,lang):'';return `<li class="tier${x.contact?' tier--contact':''}"><span class="tier__name">${esc(L(p.name,lang))}</span><span class="tier__price">${esc(x.text)}</span><span class="tier__cycle">${x.contact?'':esc(t(lang,'pricing.perMonth'))}</span>${h?`<p class="tier__gist">${bidi(h,lang)}</p>`:''}</li>`}).join('');const detail=plans.map(p=>{const x=priceOf(p),g=p.what_you_get||{};const q=(g.quotas||[]).slice(0,3).map(v=>`<div class="expand__quota"><span>${esc(L(v.label,lang))}</span><b>${esc(L(v.value,lang))} ${esc(L(v.unit,lang))}</b></div>`).join('');const yes=(g.unlocked_features||[]).slice(0,4).map(v=>`<li>${icon('check')}<span>${bidi(L(v,lang),lang)}</span></li>`).join('');const no=(p.not_included||[]).slice(0,3).map(v=>`<li>${icon('x')}<span>${bidi(L(v,lang),lang)}</span></li>`).join('');return `<div class="expand__tier"><h4>${esc(L(p.name,lang))}<span>${esc(x.text)}</span></h4>${q?`<span class="expand__label">${esc(t(lang,'tool.quotas'))}</span>${q}`:''}${yes?`<span class="expand__label">${esc(t(lang,'tool.unlocked'))}</span><ul class="expand__list is-yes">${yes}</ul>`:''}${no?`<span class="expand__label">${esc(t(lang,'tool.notIncluded'))}</span><ul class="expand__list is-no">${no}</ul>`:''}</div>`}).join('');return `<article class="ledger__row spot tilt3d sweep reveal"><header class="ledger__head"><span class="ledger__logo">${logo?`<img src="${escAttr(logo)}" alt="" width="30" height="30" loading="lazy" decoding="async">`:`<span class="monogram" aria-hidden="true">${esc(L(tool.name,lang).trim().charAt(0))}</span>`}</span><div class="ledger__id"><h3><a href="${escAttr(href)}">${esc(L(tool.name,lang))}</a></h3><p class="ledger__tag">${bidi(L(tool.tagline,lang),lang)}</p></div><div class="ledger__marks"><span class="badge badge-accent">${esc(t(lang,`category.${tool.category}`))}</span>${tool.ratings?.overall?`<span class="badge badge-success"><span class="u-mono">${esc(tool.ratings.overall)}</span> / 5</span>`:''}</div></header><ol class="tiers">${tiers}</ol><details class="ledger__more"><summary>${esc(t(lang,'tool.pricing'))}${icon('chevron')}</summary><div class="expand">${detail}</div><a class="btn btn-ghost btn-sm u-mt-16" href="${escAttr(href)}">${esc(t(lang,'catalog.open'))}${icon('arrow','btn-arrow')}</a></details></article>`}
export function renderHome(ctx,lang,tools){const{config,t,defaultLang}=ctx;const planCount=tools.reduce((s,x)=>s+(x.pricing?.plans?.length||0),0);const featured=[...tools].sort((a,b)=>(b.ratings?.overall??0)-(a.ratings?.overall??0)).slice(0,8);const steps=[1,2,3].map(n=>`<article class="card step-card spot tilt3d reveal"><h3>${esc(t(lang,`home.how${n}t`))}</h3><p>${esc(t(lang,`home.how${n}d`))}</p></article>`).join('');const main=`<section class="hero scene grain"><div class="scene__layer scene__layer--back" aria-hidden="true"><div class="dotfield"></div></div><div class="scene__layer scene__layer--mid" aria-hidden="true"><div class="dotfield dotfield--floor"></div></div><div class="container hero__grid scene__body stage"><div class="hero-inner"><p class="kicker">${esc(t(lang,'home.heroKicker')||L(config.siteName,lang))}</p><h1><span>${esc(t(lang,'home.heroA'))}</span><span class="grad-text">${esc(t(lang,'home.heroB'))}</span></h1><p class="lead">${esc(t(lang,'home.lead'))}</p><div class="hero-actions"><a class="btn btn-primary" href="#ledger">${esc(t(lang,'home.ctaExplore'))}${icon('arrow','btn-arrow')}</a><a class="btn btn-ghost" href="${escAttr(urlFor(lang,defaultLang,'contact.html'))}">${icon('chat')}${esc(t(lang,'home.ctaContact'))}</a></div></div><div class="orb" data-orb aria-hidden="true"><div class="orb__halo"></div><span class="orb__ring"></span><span class="orb__ring orb__ring--b"></span><canvas class="orb__canvas"></canvas></div></div><div class="container"><div class="stats"><div class="stat reveal"><b class="u-mono">${tools.length}</b><span>${esc(t(lang,'home.statTools'))}</span></div><div class="stat reveal"><b class="u-mono">${planCount}</b><span>${esc(t(lang,'home.statPlans'))}</span></div><div class="stat reveal"><b class="u-mono">2</b><span>${esc(t(lang,'home.statLangs'))}</span></div></div></div></section><section class="section" id="ledger"><div class="container"><div class="section-head reveal"><p class="eyebrow">${esc(t(lang,'nav.catalog'))}</p><h2>${esc(t(lang,'home.ledgerTitle')||t(lang,'home.featured'))}</h2><p class="muted">${esc(t(lang,'home.ledgerLead')||t(lang,'home.featuredLead'))}</p></div><div class="ledger">${featured.map(x=>renderLedgerRow(ctx,lang,x)).join('')}</div>${tools.length>featured.length?`<a class="btn btn-ghost u-mt-32" href="${escAttr(urlFor(lang,defaultLang,'catalog.html'))}">${esc(t(lang,'catalog.title'))}${icon('arrow','btn-arrow')}</a>`:''}</div></section><section class="section"><div class="container"><div class="section-head reveal"><p class="eyebrow">${icon('list')}</p><h2>${esc(t(lang,'home.how'))}</h2><p class="muted">${esc(t(lang,'home.howLead'))}</p></div><div class="steps">${steps}</div></div></section>`;return renderLayout(ctx,{lang,relPath:'index.html',navId:'home',entry:'entry-home.js',title:t(lang,'home.title'),description:t(lang,'home.desc'),main,jsonLd:{'@context':'https://schema.org','@type':'WebSite',name:L(config.siteName,lang),url:config.baseUrl+urlFor(lang,defaultLang,'index.html'),inLanguage:lang,description:t(lang,'home.desc')}})}