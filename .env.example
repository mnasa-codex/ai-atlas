NEXT_PUBLIC_BASE_PATH=/ai-atlas
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Optional future live data providers (keep secrets server-side; never expose private exchange keys to the browser)
